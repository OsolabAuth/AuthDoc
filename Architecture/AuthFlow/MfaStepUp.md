# 多要素認証と強化認可フロー

## 目的

メールコードまたはAuthenticatorアプリで追加認証を行い、退会やパスワード変更などの高リスク操作だけに短時間の強化認可を要求する。

## フロー

1. ログイン済みユーザーがMFA開始APIを呼び出す。
2. メールコード送信、またはAuthenticator設定/検証を行う。
3. 検証成功時に5分程度のstep-up状態を発行する。
4. 高リスクAPIはstep-up状態が有効な場合のみ処理する。

## 注意点

- 通常ログインセッションとは別にstep-up状態を管理する。
- 失敗回数制限と監査ログを前提にする。
